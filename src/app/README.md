# Application boundary

This directory owns the React shell, route composition, page-level content, and global visual foundation. Route pages may call feature modules as those modules become real; business rules stay in `src/domain`.
